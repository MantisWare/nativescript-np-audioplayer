export declare function fixPath(pathStr: string): any;
export declare enum AudioPlayerState {
    Unloaded = 0,
    Loading = 1,
    Loaded = 2,
    Playing = 3,
    Paused = 4,
    Errored = 5,
}
export { IAudioPlayer } from './audioplayer';
